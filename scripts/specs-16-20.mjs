/**
 * All blocks in this range have been promoted to hand-written, implemented blocks (see blocks/*),
 * so nothing here is generated any more. The empty array keeps scaffold.mjs's spread and its
 * duplicate-rank check valid.
 */

/** @type {import("./lib/generate.mjs").BlockSpec[]} */
export const SPECS = [];
