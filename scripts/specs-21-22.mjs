/**
 * Blocks 21–22 (agent-memory, feature-flags) were promoted to hand-written, implemented blocks
 * (see blocks/agent-memory and blocks/feature-flags), so nothing in this range is generated any
 * more. The empty array keeps `scaffold.mjs`'s spread and duplicate-rank check valid.
 */

/** @type {import("./lib/generate.mjs").BlockSpec[]} */
export const SPECS = [];
