{
  "slug": "analytics",
  "rank": 24,
  "name": "Event Analytics",
  "summary": "Event ingest, sessionization, and a funnel, retention, and cohort query pack over your own Postgres.",
  "schema": "blocks_analytics",
  "billing": "free",
  "depth": "implemented",
  "capabilities": [
    "postgres"
  ],
  "dependsOn": [
    "queue"
  ],
  "env": [
    {
      "name": "DATABASE_URL",
      "description": "Branch connection string. Injected automatically by Neon.",
      "required": true,
      "injected": true
    },
    {
      "name": "ANALYTICS_SESSION_GAP_MINUTES",
      "description": "Inactivity gap that ends a session. 30 is conventional; changing it changes every historical session count.",
      "required": false,
      "default": "30"
    },
    {
      "name": "ANALYTICS_MAX_BATCH",
      "description": "Events accepted per ingest request.",
      "required": false,
      "default": "1000"
    },
    {
      "name": "ANALYTICS_RETENTION_DAYS",
      "description": "Days raw events are kept. Rollups outlive them, so long-range trends survive the purge.",
      "required": false,
      "default": "400"
    },
    {
      "name": "NEON_BLOCKS_TRIGGER_SECRET",
      "description": "Shared secret authenticating trigger delivery. Neon does not sign trigger POSTs.",
      "required": false
    }
  ],
  "triggers": [
    {
      "type": "schedule",
      "cron": "13 1 * * *",
      "functionPath": "/rollup",
      "description": "Sessionize new events and refresh daily aggregates. Off-peak, because it scans a day of raw events."
    }
  ],
  "operations": [
    {
      "id": "track",
      "title": "POST /track",
      "description": "Ingest a batch of events, idempotently.",
      "route": "/track",
      "recommended": true
    },
    {
      "id": "rollup",
      "title": "POST /rollup",
      "description": "Cron. Sessionize and aggregate.",
      "route": "/rollup",
      "recommended": true
    },
    {
      "id": "funnel",
      "title": "GET /funnel",
      "description": "Ordered-step funnel conversion.",
      "route": "/funnel",
      "recommended": true
    },
    {
      "id": "retention",
      "title": "GET /retention",
      "description": "Cohort retention by week.",
      "route": "/retention",
      "recommended": true
    },
    {
      "id": "health",
      "title": "GET /health",
      "description": "Liveness and readiness, backed by the block's v_status view.",
      "route": "/health",
      "recommended": false
    }
  ]
}
