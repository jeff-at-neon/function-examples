{
  "slug": "api-edge",
  "rank": 12,
  "name": "API keys & limits",
  "summary": "Hashed API keys, per-tenant rate limits, and idempotency for your API",
  "schema": "blocks_api_edge",
  "billing": "free",
  "depth": "implemented",
  "capabilities": [
    "postgres",
    "neon_auth"
  ],
  "dependsOn": [],
  "env": [
    {
      "name": "DATABASE_URL",
      "description": "Branch connection string. Injected automatically by Neon.",
      "required": true,
      "injected": true
    },
    {
      "name": "API_KEY_PREFIX",
      "description": "Human-readable key prefix, e.g. nb_live. Makes leaked keys identifiable in logs and greppable in code.",
      "required": false,
      "default": "nb_live"
    },
    {
      "name": "API_RATE_WINDOW_SECONDS",
      "description": "Fixed window length. Shorter windows reduce burst but increase write volume.",
      "required": false,
      "default": "60"
    },
    {
      "name": "API_DEFAULT_RATE_LIMIT",
      "description": "Requests per window when a key has no explicit limit.",
      "required": false,
      "default": "1000"
    },
    {
      "name": "API_IDEMPOTENCY_TTL_HOURS",
      "description": "How long a stored idempotent response is replayed before the key is reusable.",
      "required": false,
      "default": "24"
    },
    {
      "name": "NEON_BLOCKS_TRIGGER_SECRET",
      "description": "Shared secret authenticating trigger delivery. Neon does not sign trigger POSTs.",
      "required": false
    }
  ],
  "triggers": [
    {
      "type": "schedule",
      "cron": "11 * * * *",
      "functionPath": "/sweep",
      "description": "Prune expired rate windows and idempotency records. Without it both tables grow without bound."
    }
  ],
  "operations": [
    {
      "id": "keys",
      "title": "POST /keys",
      "description": "Create a key. Returns the plaintext exactly once.",
      "route": "/keys",
      "recommended": true
    },
    {
      "id": "keys-id",
      "title": "DELETE /keys/:id",
      "description": "Revoke a key.",
      "route": "/keys/:id",
      "recommended": true
    },
    {
      "id": "verify",
      "title": "POST /verify",
      "description": "Verify a key and consume rate budget.",
      "route": "/verify",
      "recommended": true
    },
    {
      "id": "sweep",
      "title": "POST /sweep",
      "description": "Cron. Prune expired windows and idempotency records.",
      "route": "/sweep",
      "recommended": true
    },
    {
      "id": "health",
      "title": "GET /health",
      "description": "Liveness and readiness, backed by the block's v_status view.",
      "route": "/health",
      "recommended": false
    }
  ]
}
